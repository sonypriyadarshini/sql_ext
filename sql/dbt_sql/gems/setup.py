
from setuptools import setup, find_packages
setup(
    name = 'dbt_sql',
    version = '0.0.1',
    packages = ['dbt_sql'],
    package_dir = {'dbt_sql': '.'},
    description = '',
    install_requires = [],
)

