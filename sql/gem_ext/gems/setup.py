
from setuptools import setup, find_packages
setup(
    name = 'sql_gem_ext',
    version = '0.0.1',
    packages = ['sql_gem_ext'],
    package_dir = {'sql_gem_ext': '.'},
    description = '',
    install_requires = [],
)

